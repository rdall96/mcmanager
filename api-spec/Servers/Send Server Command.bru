meta {
  name: Send Server Command
  type: http
  seq: 15
}

post {
  url: {{base_url}}/api/v1/servers/{{server_id}}/command
  body: text
  auth: inherit
}

body:text {
  /weather clear
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
