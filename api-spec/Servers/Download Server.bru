meta {
  name: Download Server
  type: http
  seq: 17
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}/download
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
