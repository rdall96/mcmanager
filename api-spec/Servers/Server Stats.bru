meta {
  name: Server Stats
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}/stats
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
