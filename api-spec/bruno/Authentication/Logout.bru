meta {
  name: Logout
  type: http
  seq: 2
}

get {
  url: {{base_url}}/auth/logout
  body: none
  auth: inherit
}

script:post-response {
  if (res.status == 204) {
    bru.setEnvVar("token", "");
  }
}
