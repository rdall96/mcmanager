meta {
  name: Server
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
