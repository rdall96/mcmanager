meta {
  name: Banned Players
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}/players/banned
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
