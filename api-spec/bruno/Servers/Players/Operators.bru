meta {
  name: Operators
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}/players/operators
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
