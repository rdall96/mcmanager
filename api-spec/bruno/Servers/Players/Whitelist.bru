meta {
  name: Whitelist
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}/players/whitelist
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
