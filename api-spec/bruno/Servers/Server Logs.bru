meta {
  name: Server Logs
  type: http
  seq: 14
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}/logs?tail=10
  body: none
  auth: inherit
}

params:query {
  tail: 10
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
