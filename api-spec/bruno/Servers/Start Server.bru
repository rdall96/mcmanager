meta {
  name: Start Server
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/v1/servers/{{server_id}}/start
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("server_id") == "") {
    throw "Missing server ID";
  }
}
