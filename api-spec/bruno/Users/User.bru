meta {
  name: User
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/users/{{user_id}}
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("user_id") == "") {
    throw "Missing user ID";
  }
}
