meta {
  name: User Role
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/v1/users/{{user_id}}/role
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("user_id") == "") {
    throw "Missing user ID";
  }
}
