meta {
  name: Profile
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/users/profile
  body: none
  auth: inherit
}

script:post-response {
  if (res.status == 200) {
    bru.setEnvVar("user_id", res.body.id);
  }
}
