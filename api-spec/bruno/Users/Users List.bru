meta {
  name: Users List
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/users
  body: none
  auth: inherit
}

script:post-response {
  if (res.status == 200) {
    bru.setEnvVar("user_id", res.body[0].id);
  }
}
