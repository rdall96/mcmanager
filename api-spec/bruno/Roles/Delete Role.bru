meta {
  name: Delete Role
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/v1/roles/{{role_id}}
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("role_id") == "") {
    throw "Missing role ID";
  }
}
