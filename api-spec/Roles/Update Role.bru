meta {
  name: Update Role
  type: http
  seq: 3
}

put {
  url: {{base_url}}/api/v1/roles/{{role_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Default (updated)"
  }
}

script:pre-request {
  if (bru.getEnvVar("role_id") == "") {
    throw "Missing role ID";
  }
}
