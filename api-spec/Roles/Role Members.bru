meta {
  name: Role Members
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/v1/roles/{{role_id}}/members
  body: none
  auth: inherit
}

script:pre-request {
  if (bru.getEnvVar("role_id") == "") {
    throw "Missing role ID";
  }
}
