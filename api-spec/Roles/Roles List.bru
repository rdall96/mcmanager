meta {
  name: Roles List
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/roles
  body: none
  auth: inherit
}

script:post-response {
  if (res.status == 200 && res.body[0] != null) {
    bru.setEnvVar("role_id", res.body[0].id);
  }
}
