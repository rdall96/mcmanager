stages:
  - build
  - release


variables:
  # Name of the public image
  IMAGE_NAME: "rdall96/mcmanager"
  # Commit tags regex to determine what pipeline to run
  DEV_TAG: '/^dev-\d.\d.\d$/'
  RELEASE_TAG: '/^\d.\d.\d$/'
  # Name of the tag when doing manual builds
  CUSTOM_TAG: ""
  # Use TLS https://docs.gitlab.com/ee/ci/docker/using_docker_build.html#tls-enabled
  DOCKER_HOST: tcp://docker:2376
  DOCKER_TLS_CERTDIR: "/certs"


default:
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  before_script:
    - docker info
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY


# Development build - On new commits
dev-build:
  stage: build
  script:
    # Build the image and upload to to the private registry
    - |
      docker build -t $CI_REGISTRY/$IMAGE_NAME:$CI_COMMIT_SHORT_SHA . \
      --build-arg C_OPTIMIZATION="-Og" \
      --build-arg BUILD_TYPE="debug"
    - docker push $CI_REGISTRY/$IMAGE_NAME:dev-$CI_COMMIT_SHORT_SHA
  rules:
    # only run this job when a new commit is created and we don't have a tag
    - if: $CI_COMMIT_TAG =~ $RELEASE_TAG
      when: never
    # allow build tagging in merge requests (optional)
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: manual
      allow_failure: true
    # and if we have changes in relevant files when pushing a new commit
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - Dockerfile
        - Sources/*
        - Package.*
      when: always


# Release build - New tags matching RELEASE_TAG
release-build:
  stage: build
  script:
    # Build and upload to private registry
    - docker build -t $CI_REGISTRY/$IMAGE_NAME:$CI_COMMIT_TAG .
    - docker push $CI_REGISTRY/$IMAGE_NAME:$CI_COMMIT_TAG
  rules:
    - if: $CI_COMMIT_TAG =~ $RELEASE_TAG


# Release - Upload the image to dockerhub
release-dockerhub:
  stage: release
  script:
    # Download the newly built image
    - docker pull $CI_REGISTRY/$IMAGE_NAME:$CI_COMMIT_TAG
    # Login into dockerhub
    - docker login -u $DOCKERHUB_USER -p $DOCKERHUB_TOKEN
    # Tag the image as latest and push
    - docker tag $CI_REGISTRY/$IMAGE_NAME:$CI_COMMIT_TAG $CI_REGISTRY/$IMAGE_NAME:latest
    - docker push $IMAGE_NAME:$CI_COMMIT_TAG
    - docker push $IMAGE_NAME:latest
  rules:
    - if: $CI_COMMIT_TAG =~ $RELEASE_TAG


release-docs:
  stage: release
  image: alpine:latest
  script:
    - apk update && apk add curl
    - |
      curl --location \
      --output /usr/local/bin/release-cli \
      "https://gitlab.com/api/v4/projects/gitlab-org%2Frelease-cli/packages/generic/release-cli/latest/release-cli-linux-amd64"
    - chmod +x /usr/local/bin/release-cli
    - echo 'Creating release $CI_COMMIT_TAG'
  rules:
    - if: $CI_COMMIT_TAG =~ $RELEASE_TAG
  release:
    tag_name: $CI_COMMIT_TAG
    name: 'Release $CI_COMMIT_TAG'
    # TODO: Find a way to add a direct link to the image
    description: '[DockerHub - MCManager](https://hub.docker.com/repository/docker/rdall96/mcmanager/general)\n$IMAGE_NAME:$CI_COMMIT_TAG\nAutomated release (maintainer to fill in details)'


# Manual build - On custom build tags (for testing purposes)
manual-build:
  stage: build
  script:
    # Build and push to the private registry
    - docker build -t $CI_REGISTRY/$IMAGE_NAME:$CUSTOM_TAG .
    - docker push $CI_REGISTRY/$IMAGE_NAME:$CUSTOM_TAG
  rules:
    - if: $CUSTOM_TAG != null && $CUSTOM_TAG != ""
